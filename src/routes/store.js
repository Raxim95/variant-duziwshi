import { writable } from "svelte/store";

export const menu = writable("kategoriyalar");
export const cat_id = writable(131);
export const variantlar_sani = writable(50);
export const variantlar = writable("");
